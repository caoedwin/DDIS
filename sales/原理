幂等性工作原理总结
什么是幂等性？
幂等性是指同一个操作执行多次，产生的结果与执行一次完全相同。

在支付系统中的重要性
支付回调可能因为网络问题被重复发送，如果没有幂等性保护，会导致：

同一订单被多次发货 → 库存超卖、资金损失

同一笔钱被多次记账 → 账目混乱

本系统的幂等实现（三层防护）
┌─────────────────────────────────────────────────────────────┐
│                    第一层：Redis 缓存                        │
│  key: "idempotent:{idempotent_key}"                        │
│  作用：快速返回已处理的结果，减少数据库查询                   │
│  命中 → 直接返回，不查数据库                                │
└─────────────────────────────────────────────────────────────┘
                              ↓ 未命中
┌─────────────────────────────────────────────────────────────┐
│                  第二层：数据库唯一约束                       │
│  表：IdempotentKey                                          │
│  字段：idempotent_key (UNIQUE)                              │
│  作用：数据库层面保证同一键只能插入一次                       │
│  插入成功 → 执行支付逻辑                                     │
│  插入失败（重复键）→ 查询已有状态并返回                      │
└─────────────────────────────────────────────────────────────┘
                              ↓ 插入成功
┌─────────────────────────────────────────────────────────────┐
│                  第三层：状态机乐观锁                        │
│  更新订单状态时：WHERE id=xxx AND status='期望值'            │
│  作用：防止并发修改订单状态                                  │
│  影响行数 = 0 → 状态已被其他请求改变，拒绝操作              │
└─────────────────────────────────────────────────────────────┘
幂等键的生成
text
幂等键 = 订单号 + 支付方式 + 随机字符串
例如：ORD20260101120000_WECHAT_a1b2c3d4e5f6
回调处理的幂等
text
回调幂等键 = "pay_callback_" + 交易号
例如：pay_callback_20260101120000123456789
回调机制总结
text
┌────────────┐    支付请求    ┌────────────┐
│   用户     │ ────────────→ │  支付平台   │
│  (浏览器)  │               │ (微信/支付宝)│
└────────────┘               └────────────┘
     │                              │
     │                              │ 用户支付完成
     │                              ↓
     │                        ┌────────────┐
     │                        │  异步通知   │
     │                        │ (POST回调) │
     │                        └────────────┘
     │                              │
     │                              │ 通知 /sales/api/payment_callback/
     │                              ↓
     │                        ┌────────────┐
     │                        │  你的系统   │
     │                        │ 验证签名   │
     │                        │ 幂等检查   │
     │                        │ 更新订单   │
     │                        │ 扣减库存   │
     │                        └────────────┘
     │                              │
     │                              │ 返回 'success'
     │                              ↓
     │                        ┌────────────┐
     │                        │  支付平台   │
     │                        │ 停止重试   │
     └────────────────────────┘
回调的核心作用：支付平台告诉你"钱到账了"，这是发货的唯一依据。你绝对不能根据前端支付成功的页面来判断是否发货，因为前端可能被篡改。只有收到支付平台签名的回调通知，才能确认收款成功。